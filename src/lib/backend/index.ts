// TODO: make use of both interpreter and connection at the same time to create one object
import { BLE } from "./connection/ble.svelte";
import { Serial } from "./connection/serial.svelte";

export { BLE, Serial };
